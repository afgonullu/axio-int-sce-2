{
  "name": "rbac-route-guard-scenario",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
